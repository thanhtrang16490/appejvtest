# Nixpacks Configuration for appejv-app (Next.js)

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"

[variables]
PORT = "3000"
NODE_ENV = "production"
